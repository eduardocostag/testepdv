  return <><Header title="Pedidos" desc="Crie, acompanhe e gerencie toda a operaÃ§Ã£o"/><div className="cols"><section className="card"><h3>Novo pedido</h3><form className="cardForm" onSubmit={createOrder}><div className="inlineRow"><label>Origem<select value={origin} onChange={e=>setOrigin(e.target.value)}><option value="HALL">SalÃ£o</option><option value="DELIVERY">Delivery</option><option value="IFOOD">iFood</option><option value="WHATSAPP">WhatsApp</option></select></label>{origin==='HALL'?<label>Mesa<select value={tableId} onChange={e=>setTableId(e.target.value)}><option value="">Selecione</option>{tables.map((t:Any)=><option key={t.id} value={t.id}>Mesa {t.number} - {statusLabel(t.status)}</option>)}</select></label>:<label>Cliente<input value={customerName} onChange={e=>setCustomerName(e.target.value)} placeholder="Nome do cliente"/></label>}</div>{origin !== 'HALL' && <div className="inlineRow"><label>Telefone<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Telefone"/></label></div>}<div className="orderLines">{items.map((item:any,index:number)=><div className="orderLine" key={index}><select value={item.productId} onChange={e=>updateLine(index,'productId',e.target.value)}><option value="">Selecionar produto</option>{products.map((p:Any)=><option key={p.id} value={p.id}>{p.name} - {money(p.price)}</option>)}</select><input type="number" min="1" value={item.quantity} onChange={e=>updateLine(index,'quantity',Number(e.target.value))} placeholder="Qtd"/><input value={item.notes} onChange={e=>updateLine(index,'notes',e.target.value)} placeholder="ObservaÃ§Ãµes"/><button type="button" className="secondary" onClick={()=>removeLine(index)}>Remover</button></div>)}</div><div className="actionsWrap"><button type="button" className="secondary" onClick={addLine}>Adicionar item</button><button className="primary" type="submit">Enviar pedido</button></div></form></section><section className="card"><h3>SimulaÃ§Ã£o</h3><p>Use o botÃ£o abaixo para receber um pedido mock iFood diretamente no fluxo.</p><button className="primary" onClick={async()=>{await api.post('/ifood/simulate-order',{}); load();}}>Simular pedido iFood</button></section></div>{loading && <div className="card">Carregando pedidos...</div>}<div className="kanban">{['NEW','ACCEPTED','PREPARING','READY','OUT_FOR_DELIVERY'].map(st=><section className="card" key={st}><h3>{statusLabel(st)}</h3>{orders.filter(o=>o.status===st).map(o=><div className="order" key={o.id}><div className="orderHeader"><div><b>#{o.id.slice(-5)} Â· {originLabel[o.origin]||o.origin}</b><small>{o.table?`Mesa ${o.table.number}`:o.customer?.name||o.customer?.phone}</small></div><span className="badge">{o.origin}</span></div>{o.items.map((i:Any)=><p key={i.id}>{i.quantity}x {i.product.name} <small>{money(i.unitPrice)}</small></p>)}<p><strong>Total:</strong> {money(o.total)}</p><p><strong>Pagamento:</strong> {o.payments?.length ? `${o.payments.length} transaÃ§Ã£o(Ãµes)` : 'Sem pagamento'}</p>{o.origin !== 'HALL' && <div className="formRow"><select defaultValue="" onChange={e=>assignCourier(o.id,e.target.value)}><option value="">Atribuir motoboy</option>{couriers.map((c:Any)=><option key={c.id} value={c.id}>{c.name} - {courierStatusLabel[c.status]||c.status}</option>)}</select></div>}<div className="actions">{nextStatusActions[o.status].map((next:string)=><button key={next} className="secondary" onClick={()=>updateStatus(o.id,next)}>{statusLabel(next)}</button>)}<button className="secondary" onClick={()=>updateStatus(o.id,'CANCELED')}>Cancelar</button></div></div>)}</section>)}</div></>}